export enum WithdrawalStatus {
  initial = 'initial',
  invalid = 'invalid',
  incomplete = 'incomplete',
  declined = 'declined',
  cancelled = 'cancelled',
  succeeded = 'succeeded',
}
export enum Currencies {
  BGN = 'BGN',
  USD = 'USD',
  EUR = 'EUR',
}
