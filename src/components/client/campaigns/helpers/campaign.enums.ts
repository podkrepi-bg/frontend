export enum CampaignState {
  draft = 'draft',
  active = 'active',
  partially_financed = 'partially_financed',
  suspended = 'suspended',
  complete = 'complete',
  blocked = 'blocked',
  paused = 'paused',
  deleted = 'deleted',
}

export enum StatisticsGroupBy {
  DAY = 'day',
  WEEK = 'week',
  MONTH = 'month',
}
