export type BootcampTypeFormData = {
  firstName: string
  lastName: string
  city: string
}

export type BootcampResponse = {
  id: string
  firstName: strings
  lastName: string
  city: string
}

export type DeleteMany = {
  ids: string[]
}
