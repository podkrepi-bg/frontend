export enum BankDonationStatus {
  unrecognized = 'unrecognized',
  imported = 'imported',
  importFailed = 'importFailed',
}

export enum BankTransactionType {
  debit = 'debit',
  creadit = 'credit',
}
