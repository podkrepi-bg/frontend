export type CarDataType = {
  brand: string
  model: string
  year: number
  engine: string
  price: number
}

export type CarResponse = {
  id: UUID
  brand: string
  model: string
  year: number
  engine: string
  price: number
}
