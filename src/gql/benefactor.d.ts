export type BenefactorFormData = {
  // id: string
  extCustomerId: string
  // createdAt: string
  // updatedAt: string
  person: string
}

export type BenefactorInput = {
  // id: string
  extCustomerId: string
  // createdAt: string
  // updatedAt: string
  person: string
}

export type BenefactorResponse = {
  id: string
  // personId: string
  extCustomerId: strings
  createdAt: string
  updatedAt: string
  person: string
}
