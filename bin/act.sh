#!/bin/bash
act -C ../ -s GHCR_TOKEN="${A_GHCR_TOKEN}" -s GHCR_USERNAME="${A_GHCR_USERNAME}" -s REMOTE_DOCKER_HOST="${A_REMOTE_DOCKER_HOST}" -s SSH_PUBLIC_KEY="${A_SSH_PUBLIC_KEY}" -s SSH_PRIVATE_KEY="${A_SSH_PRIVATE_KEY}" $@
